import { Glow } from './Glow';

export interface generateGlowStylesInput {
    glow?: Glow;
    colorSchemeColor?: string;
}
export declare const generateGlowStyles: ({ glow, colorSchemeColor }: generateGlowStylesInput) => {
    filter?: undefined;
} | {
    filter: string;
};
