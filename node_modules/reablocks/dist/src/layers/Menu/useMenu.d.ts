import { MenuProps } from './Menu';
import { default as React } from 'react';

type MenuOptions = {
    open?: boolean;
    onClose?: () => void;
} | undefined;
export declare const useMenu: (prop?: MenuOptions) => {
    isOpen: boolean;
    setOpen: React.Dispatch<React.SetStateAction<boolean>>;
    toggleOpen: () => void;
    Menu: (props: MenuProps) => import("react/jsx-runtime").JSX.Element;
    ref: React.MutableRefObject<any>;
};
export {};
